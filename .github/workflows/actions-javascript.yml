name: Java-Script Action flow

on:
  # push:
  #   branches:
  #     - "master"
  #     - 'feature/*' # matches branches like feature/B1 or feature/B2 but not feature/B/A1
  #     - 'feature/**' # matches branched like feature/A/B
  #     - '!feature/featA' # to ignore the branch
  #   # branches-ignore:
  #   #   - ""
  #   tags:
  #     - v1.*
  #   # tags-ignore:
  #   #   - ""
  #   paths:
  #     - '**.js'
  #     - '!filename.js'
  #   # paths-ignore:
    #   - ""


  # repository_dispatch:
  #   types: [build]
    
  # schedule:
  #   - cron: "*/5 * * * *"
  #push:
  pull_request:
    types:
      - closed
      - assigned
      - opened
      - reopened
      - unassigned

jobs:
  run-github-actions:
    runs-on: ubuntu-18.04
    steps:
      - name: list files
        run: |
          pwd
          ls -a
          ### Environment Variables ###
          echo $GITHUB_SHA
          echo $GITHUB_REPOSITORY
          echo $GITHUB_WORKSPACE
          echo "${{ github.token }}"
          #############################
      - name: Checkout
        uses: actions/checkout@v1
      - name: list files after Checkout
        run: |
          pwd
          ls -a
      - name: Simple JS Action
        id: greet
        uses: actions/hello-world-javascript-action@v1.1
        with:
          who-to-greet: Sagar
      - name: Log Greeting time
        run: echo "${{ steps.greet.outputs.time }}"